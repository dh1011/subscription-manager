version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - HOST=0.0.0.0
    volumes:
      - ./subscriptions.db:/app/subscriptions.db

volumes:
  subscriptions_data: